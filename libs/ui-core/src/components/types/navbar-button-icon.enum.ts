import { Translations } from '@store/types'

export enum NavbarButtonSetting {
    ADD = Translations.KEY.ADD as any,
    CONFIRM = Translations.KEY.CONFIRM as any,
    BACK = Translations.KEY.BACK as any,
}
